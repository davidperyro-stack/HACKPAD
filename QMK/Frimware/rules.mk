# This file is intentionally mostly empty.
# Everything QMK needs (MCU, bootloader, matrix pins, features) is
# declared in info.json since RP2040 boards use the data-driven build.
#
# Add make-time only options here if you need them later, e.g.:
# LTO_ENABLE = yes
